syntax = "proto3";

import "nanopb.proto";

message SuccessStatus {
    oneof PossibleData {
        string data = 1 [(nanopb).max_size = 255];
        string error = 2 [(nanopb).max_size = 255];
    }
}